<Window x:Class="BeautyApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BeautyApplication - Login" Height="300" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="BeautyApplication" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBox Grid.Row="1" x:Name="EmailTextBox" Margin="0,0,0,10" Padding="5" Text="client@example.com"/>

        <PasswordBox Grid.Row="2" x:Name="PasswordBox" Margin="0,0,0,10" Padding="5"/>

        <Button Grid.Row="3" Content="Login" Width="100" Click="LoginButton_Click"/>
    </Grid>
</Window>
